{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "vercel-build": "composer install --no-dev --prefer-dist --optimize-autoloader && npm ci && npm run build && php artisan key:generate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache"
    },
    "devDependencies": {
        "@tailwindcss/postcss": "^4.2.0",
        "@tailwindcss/vite": "^4.0.0",
        "autoprefixer": "^10.4.24",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.5.6",
        "tailwindcss": "^4.2.0",
        "vite": "^7.0.7"
    }
}
